node_modules
dist
src/*.js
*.swp

# Amelia's mage is the one paid pack here, and this repo is public. The sheets
# travel as ciphertext instead: `scripts/encrypt-art.sh` writes the `.age`
# files, and the deploy decrypts them. See the Redistribution note in
# `CREDITS.md`. Everything else under `public/oboro/` is the free pack.
public/oboro/mage/*.png
