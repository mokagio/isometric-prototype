node_modules
dist
src/*.js
*.swp

# Amelia's mage is the only art here behind a paywall, and this repo is public.
# It travels as ciphertext in `art-secrets/` instead — outside `public/`, which
# Vite copies wholesale — and `npm run art:decrypt` puts it back here. See the
# Redistribution note in `CREDITS.md`.
public/oboro/mage/*.png
